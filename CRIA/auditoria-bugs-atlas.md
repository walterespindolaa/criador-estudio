# Auditoria de bugs — projeto CRIA ("Atlas")

Data: 2026-07-02 · Escopo: repo `walterespindolaa/criador-estudio` (branch `main`) — frontend React/Vite + 39 edge functions Supabase + 72 migrations.
Método: mapeamento → 6 revisores em paralelo (segurança, lógica, DB, frontend, integrações, build) → verificação adversarial de cada achado. Só entram na tabela os **CONFIRMADOS** e **PROVÁVEIS**. Falsos positivos descartados no rodapé.

Observação de ambiente: `tsc`/`vite build`/`vitest` **não** rodaram aqui (o `node_modules` do sandbox estava ilegível). Rode localmente `npx tsc -p tsconfig.app.json --noEmit && npm test && npx vite build` antes do próximo push.

---

## Tabela ranqueada (CONFIRMADOS + PROVÁVEIS)

| # | Severidade | Arquivo:linha | Bug | Evidência | Impacto | Correção proposta | Esforço |
|---|---|---|---|---|---|---|---|
| 1 | **CRÍTICO** | `supabase/functions/stripe-webhook/index.ts:124-130` e `:366-371` | Erros de DB do supabase-js são **engolidos** (não lançam exceção) e a idempotência é check-then-act. Evento é marcado como processado mesmo se o update do perfil falhar. | `await supabase.from("profiles").update({subscription_status:"active",...}).eq("id",userId)` sem checar `.error`; depois `insert billing_events` e `return 200`. | **Cliente paga e não é ativado**, sem retry (Stripe já recebeu 200). Entregas concorrentes de `invoice.paid` podem adiantar comissão de parceira (`paid_invoices_count`). Stripe LIVE. | Checar `.error` de cada mutação e lançar (deixar o Stripe retentar); inserir `billing_events` **primeiro** e tratar violação de `UNIQUE(gateway,event_id)` como duplicata. | M |
| 2 | **CRÍTICO** | `src/lib/subscription-status.ts:21-28` + `src/hooks/useTier.ts:20-23` | `deriveSubStatus`/`deriveTier` só tratam `active` e `canceled`. `past_due`/`trialing`/`incomplete` (gravados pelo próprio webhook em `:178-183`) caem no branch de trial. | Assinante com trial vencido + `past_due` → `trial_expired`; sem `trial_ends_at` → `blocked`. `ProtectedRoute.tsx:19-22` manda pro paywall. Gate server-side `has_access()` também só aceita `active`. | **Assinante pagante perde acesso no 1º cartão recusado**, sem carência, com mensagem de "trial" errada. | Adicionar casos `past_due`/`unpaid` (carência de N dias) e `trialing`/`incomplete` (tratar como ativo) em `deriveSubStatus` e `deriveTier`. | M |
| 3 | **CRÍTICO** | `src/hooks/useSocialInsights.ts:137-140` e `:63-67`; `supabase/functions/instagram-sync/index.ts:33-35` | Após a migration `20260624000008` permitir N conexões por `(user_id,provider)`, as queries de conta própria não filtram `crm_client_id IS NULL`. | `.delete().eq("user_id",user.id).eq("provider","instagram")` sem `.is("crm_client_id",null)`; `maybeSingle()` idem. | **Desconectar o próprio Instagram apaga as conexões e tokens de TODOS os clientes da gestora** (perda de dado). Sync da conta própria quebra silenciosamente (`not_connected`). | Adicionar `.is("crm_client_id", null)` nas 3 queries (o `instagram-oauth:85` já faz certo). | P |
| 4 | **CRÍTICO** | `supabase/migrations/20260630000000_rate_limiter.sql:3-8` | Tabela `rl_buckets` criada **sem RLS e sem revoke** — única tabela do repo nessa condição. | `create table ... rl_buckets (...)` e nada de `enable row level security`/`revoke` em nenhuma migration. | Com a anon key, qualquer um faz `DELETE FROM rl_buckets` e **anula o rate limit** de `bio-track`/`meta-capi`, além de ler IPs de visitantes. | SQL abaixo (habilita RLS + revoke). | P |
| 5 | **CRÍTICO** | `supabase/migrations/20260624000011_rls_hardening.sql:23`; `20260702000003_hub_cria.sql:4` | ~20 tabelas de PII/negócio (`crm_clients`, `bio_leads`, `module_entitlements`, `collabs`, `manager_profiles`, `modules`…) foram **criadas via dashboard** e não têm `CREATE TABLE` em migration. | Comentário na própria migration: *"foram criadas via dashboard"*. `alter table public.crm_clients enable RLS` e `insert into public.modules` referenciam tabelas inexistentes num banco limpo. | `supabase db reset`/ambiente novo **falha**. RLS de tabelas com e-mails/telefones (`bio_leads`) e gate de pagamento (`module_entitlements`) **não é auditável nem reproduzível** pelo repo. | Gerar migration de baseline: `supabase db dump --schema public` do prod, versionar DDL+RLS+funções. | G |
| 6 | **ALTO** | `supabase/functions/instagram-sync/index.ts:11-14` (afeta `:40,:46,:55,:76,:86-92`) | `getJson` faz `fetch`+`r.json()` **sem checar `r.ok`** → erro da Graph API tratado como dado. | `async function getJson(url){const r=await fetch(url);return await r.json().catch(()=>({}))}` | Token expirado/rate-limit → upsert grava `followers:null` **sobrescrevendo a métrica do dia** e a função retorna `{ok:true}`; usuária vê "Insights atualizados!". | Checar `r.ok`/`body.error`, abortar o sync e retornar erro claro (marcar conexão como "reconectar"). | M |
| 7 | **ALTO** | `supabase/functions/instagram-oauth/index.ts:79` (+ ausência no repo) | Refresh de token long-lived **não implementado** — nada lê `token_expires_at` para renovar. | Grep `ig_refresh_token`/`refresh_token` no repo = só tipos e a coluna. Migration comenta `-- p/ refresh automático` que nunca foi feito. | **Toda conexão de Instagram expira em ≤60 dias** e para de sincronizar; combinado com #6, a falha é totalmente silenciosa. | Cron que chama `ig_refresh_token` para tokens perto de expirar; marcar expirados para reconexão. | M |
| 8 | **ALTO** | `src/pages/app/Criando.tsx:201-227` + `src/hooks/usePosts.ts:58-70` | `movePostStatus` dispara `fireConfetti()` e `insert audit_log` **antes** do `updatePost.mutateAsync`, e é chamado com `void` sem `.catch`; `updatePost` não tem `onError`. | `fireConfetti(); await supabase.from("audit_log").insert(...); await updatePost.mutateAsync(...)` e `void movePostStatus(...)`. | Fluxo mais usado do produto. Se o save falhar (rede/RLS): card volta sem toast, **confetti de "publicado!" e registro no audit_log disparam com o save falho**. | Mover confetti/audit para depois do save; envolver em `try/catch` com toast; adicionar `onError` em `updatePost`. | P |
| 9 | **ALTO** | `src/lib/notifications.ts:32,:93`; `src/pages/app/Plano.tsx:41,:97`; `src/pages/app/Tarefas.tsx:76,:89-91` | "Hoje" calculado com `new Date().toISOString().split("T")[0]` = data **UTC**, comparado com `scheduled_date`/`due_date` que são DATE local. | Das 21h à meia-noite BRT o "hoje" já é "amanhã". | Notificação "posts pra hoje" dispara no dia errado; cronograma da semana desloca; **tarefas que vencem hoje aparecem como atrasadas** à noite. Atinge todo usuário BR à noite. | Helper único `hojeLocalBR()` (`America/Sao_Paulo`) e trocar todos os `toISOString().split("T")[0]`. | M |
| 10 | **ALTO** | `src/components/plano/GoalsTab.tsx:324-334` | `new Date("YYYY-MM-DD")` = meia-noite **UTC**; `getMonth()`/`getFullYear()` são locais. | `const d=new Date(p.scheduled_date); d.getMonth()===i && d.getFullYear()===now...` | Todo post agendado para **dia 1** de qualquer mês é contado no mês anterior no gráfico "Visão anual"; 1º de janeiro some. | Parsear como data local (`new Date(y,m-1,d)`) ou usar o helper do #9. | P |
| 11 | **ALTO** | `supabase/config.toml` (ausência) + `supabase/migrations/20260624000004_daily_cron.sql:20-27` | `daily-notifications` não está no `config.toml` → assume `verify_jwt=true`; o cron a chama só com header `x-internal-secret`, sem JWT. | `net.http_post(... headers: Content-Type + x-internal-secret ...)` — sem `Authorization`. | O gateway rejeita com **401 antes de chegar à função** → **robô diário de notificações/push não roda**. (Latente: confirmar em produção.) | Declarar `daily-notifications` e as outras 11 funções no `config.toml` com `verify_jwt=false` onde a auth é interna. | P |
| 12 | **ALTO** | `src/pages/app/Assinar.tsx:165,:222` | `disabled={isLoading}` é por-plano (`loadingPlan===plan.id`); o botão do outro plano segue clicável durante o redirect. | `const isLoading = loadingPlan === plan.id` | Duplo checkout: 2 sessões Stripe e evento de Pixel `InitiateCheckout` com plano/valor trocado. | `disabled={loadingPlan !== null}` + guard `if (loadingPlan) return`. | P |
| 13 | **ALTO** | `supabase/functions/instagram-oauth/index.ts:54-60` | Troca do token long-lived não checa `longRes.ok`; em erro cai para o token de ~1h com `token_expires_at:null` e redireciona `?ig=connected`. | `const longToken = (longJson.access_token as string) || shortToken;` | Usuária vê "conectado" e a conexão **morre em 1 hora** (mascarado pelo #6). | Checar `longRes.ok`; se falhar, não gravar e retornar erro de reconexão. | P |
| 14 | **MÉDIO** | `supabase/functions/instagram-oauth/index.ts:18,:24` + `src/hooks/useSocialInsights.ts:201-202` | O **JWT da sessão Supabase** viaja no parâmetro `state` da URL do OAuth. | `const stateValue = clientId ? \`${token}::${clientId}\` : token;` | JWT vaza em histórico do navegador, `Referer` e logs do Meta/gateway → sequestro de sessão enquanto válido. | `state` = nonce aleatório de curta duração persistido server-side, mapeado ao usuário. | M |
| 15 | **MÉDIO** | `supabase/functions/create-checkout/index.ts:110-117` | `partner_code` aplicado com `.ilike("coupon_code", rawCode)` — o comentário diz "match exato" mas `ilike` interpreta `%`/`_`. | `.ilike("coupon_code", rawCode)` | `partner_code:"%"` casa **qualquer** parceiro aprovado → aplica cupom/desconto que não é do usuário. | Trocar por `.eq()` ou escapar `%` e `_`. | P |
| 16 | **MÉDIO** | `supabase/functions/criapost-media-delete/index.ts:25,:29-31` + `bunny-create-video/index.ts:27,41` | Delete no Bunny com `.catch(()=>{})` (não checa status) e a ref do banco é apagada mesmo assim. Além disso o front manda `scope:"criapost"` que a função de criar **ignora** (usa sempre library Stream), enquanto o delete usa library Criapost. | `await fetch(...DELETE...).catch(()=>{}); await svc.from("external_media_refs").delete()...` | Vídeo/arquivo **órfão cobrado pra sempre** no Bunny; se as libraries divergirem, thumbnail 404 e delete no lugar errado. | Checar `response.ok` antes de apagar a ref; unificar a library (respeitar `scope` ou remover o parâmetro morto). | M |
| 17 | **MÉDIO** | `src/components/kanban/PostEditor.tsx:1210` (handler `:843`) e `:481-487` | Botão "Criar/Salvar" só desabilita com título vazio (não durante a mutação); auto-save engole erro (`catch{ setAutoSaveStatus(null) }`). | `disabled={!title.trim()}`; `catch { setAutoSaveStatus(null); }` | Double-submit cria **posts duplicados**; auto-save falho some o indicador "Salvando…" e o usuário perde texto ao fechar o drawer. | `disabled` também durante `isPending`; no catch do auto-save, toast + manter estado de "não salvo". | P |
| 18 | **MÉDIO** | `src/components/accounts/CriaPostBoard.tsx:263` + `src/hooks/useCriaPost.ts:172-175` | Excluir post do cliente em 1 clique, **sem confirmação** e o hook `remove` é o único sem `onError`. | `onClick={() => remove.mutate(p.id)}` | Exclusão acidental irreversível de conteúdo do cliente; se falhar, nada na tela. | AlertDialog de confirmação (como o "mover status" já tem em `:325`) + `onError` com toast. | P |
| 19 | **MÉDIO** | `supabase/functions/stripe-webhook/index.ts:48-50` e `meta-capi/index.ts:81-85` | `fetch` ao Graph do Meta **sem timeout** (`AbortController`); `sendMetaPurchase` roda **inline** no handler de `checkout.session.completed`. | `await fetch("https://graph.facebook.com/v19.0/...events?...",{method:"POST",...})` | Hang do Graph atrasa a resposta ao Stripe além do timeout dele → redelivery → junto com #1 gera efeitos duplicados. | Usar o `aiFetch`/`AbortController` já existente no repo; idealmente disparar o CAPI fora do caminho crítico. | P |
| 20 | **MÉDIO** | `supabase/functions/instagram-sync/index.ts:47-54` e `src/components/accounts/CronogramaBoard.tsx:163-177` | N+1: até ~30 `upsert` sequenciais por sync; 2 queries por item aprovado ao converter. `CronogramaBoard` ainda não é idempotente no retry (`:158-185`). | `for (const v of row.values){ await admin...upsert(...) }` | Latência e custo; no `CronogramaBoard`, falha parcial + "tente de novo" **duplica posts** (retry filtra por `!converted_post_id`). | Upsert/insert em lote (`onConflict` já existe); marcar convertidos antes de reprocessar. | M |
| 21 | **BAIXO** | `.env` (rastreado) + `.gitignore` | `.env` está commitado e não é ignorado. Hoje contém só chaves `VITE_*` públicas — risco atual baixo, padrão errado. | `git ls-files` lista `.env`; `.gitignore` não cobre. | Qualquer secret de servidor colado ali no futuro vai pro histórico. | `echo ".env" >> .gitignore && git rm --cached .env`. | P |
| 22 | **BAIXO** | `bun.lock` + `bun.lockb` + `package-lock.json` | Três lockfiles divergentes commitados. | Datas diferentes; formatos bun+npm. | Build não-determinístico (Vercel/Lovable escolhe o gerenciador pelo lockfile presente). | Manter um só (bun.lock se o pipeline usa bun) e deletar os outros. | P |
| 23 | **BAIXO** | `src/components/shared/CoverHeader.tsx:16` (usado em `Criando.tsx:526,862`) | Backlog confirmado: `truncate` + fonte display **itálica** com `leading-[0.96]`/`overflow-hidden` **corta o título** da coluna do kanban. | `className="... leading-[0.96] ... truncate ..."` | Títulos de capa longos e a inclinação dos glifos itálicos são clipados nas colunas estreitas. | Remover `truncate` (ou usar `line-clamp`), aumentar `leading` e `padding` vertical. | P |
| 24 | **BAIXO** | `src/pages/app/Autopilot 2.tsx` | Arquivo duplicado (conteúdo divergente de `Autopilot.tsx`), sem import/rota. | Nenhuma referência no repo. | Armadilha: alguém edita a cópia errada e "o fix não aparece". | Deletar. | P |
| 25 | **BAIXO** | `src/pages/Landing.tsx:728,:732-735` | "Contato" e os 4 ícones sociais do footer usam `href="#"`. | `<a href="#" ...>` | Visitante da landing de vendas clica em contato/social e não acontece nada. | Apontar para links reais ou remover. | P |
| 26 | **BAIXO** | `src/pages/BioPage.tsx:543,:564-566` | Form público de captação sem `label`/`aria-label` (só placeholder) e erro via `alert()` nativo. | `<input ... placeholder="Seu nome" .../>`; `catch { alert("...") }` | Acessibilidade ruim na página pública da criadora; UX de erro tosca. | Adicionar labels acessíveis e trocar `alert` por toast (Sonner). | P |
| 27 | **BAIXO** | `supabase/functions/auth-email-hook/index.ts:210,:287` | E-mail do usuário (PII/LGPD) logado em texto claro. | `console.log('...', { email: payload.data.email, run_id })` | Minimização de dados violada; PII em logs de função. | Logar só hash ou `run_id`. | P |
| 28 | **BAIXO** | `supabase/migrations/20260528204824_email_infra.sql:137-197` | Funções pgmq `SECURITY DEFINER` sem `SET search_path` (linter `function_search_path_mutable`). | `CREATE FUNCTION public.enqueue_email(...) SECURITY DEFINER AS $$...$$;` | Baixo (EXECUTE restrito a service_role), mas foge do padrão do resto do repo. | `alter function ... set search_path = public, pgmq;` (SQL abaixo). | P |

---

## Precisa de decisão do Walter

- **Tela de `past_due` (#2):** quantos dias de carência antes de cortar acesso? (Stripe costuma retentar por ~2–3 semanas.)
- **Google Drive token no body (`google-drive-connect`):** o Calendar está desativado, mas o Drive segue recebendo `accessToken` pelo body. Persistir server-side com RLS (como o Instagram) ou manter como está por ora?
- **Baseline de schema (#5):** rodar `supabase db dump` do prod para versionar as tabelas criadas via dashboard? É a correção mais trabalhosa, mas destrava `db reset` e torna o RLS auditável.

---

## Plano de correção (ordem de execução)

**Bloco 1 — destravar cobrança e parar perda de dado (fazer primeiro):**
1. `#1` Stripe webhook: checar `.error` e reordenar `billing_events` (dinheiro real, silencioso).
2. `#2` `subscription-status`/`useTier`: tratar `past_due`/`trialing` (assinante pagante sendo bloqueado).
3. `#3` `social_connections`: `.is("crm_client_id", null)` nas 3 queries (evita apagar tokens de clientes) — 1 linha cada.
4. `#4` RLS em `rl_buckets` (SQL abaixo) — 3 linhas.
5. `#11` Declarar `daily-notifications` (e as outras) no `config.toml` — confirmar se o robô diário está rodando hoje.

**Bloco 2 — Instagram e experiência principal:**
6. `#6` + `#13` `instagram-sync`/`oauth`: checar `r.ok`/`longRes.ok` (parar de gravar `null` por cima de métricas boas).
7. `#7` Refresh de token do Instagram (cron).
8. `#8` Kanban `movePostStatus`: save com `try/catch` e efeitos após o save.
9. `#9` + `#10` Helper de data local BR e trocar os `toISOString().split("T")[0]`.
10. `#12` Botão de checkout desabilitado globalmente durante o redirect.

**Bloco 3 — endurecimento e higiene (pode esperar):**
- `#5` Baseline de schema (decisão do Walter).
- `#14`–`#20` segurança/integrações (state OAuth, partner_code, Bunny, timeout Meta, N+1).
- `#21`–`#28` build/UX/LGPD (`.env`, lockfiles, header do kanban, Autopilot 2, links da landing, labels da bio, PII em log, search_path).

**Antes de qualquer push:** rodar `npx tsc -p tsconfig.app.json --noEmit && npm test && npx vite build` (não foi possível validar aqui).

---

## Falsos positivos descartados (não corrigir)

- **`course_purchases` INSERT auto-concedido** — a policy é dropada em `supabase/migrations/20260526181639_...sql:71-72`. Já corrigido.
- **Timezone em `src/hooks/useHubCria.ts:148-161`** — é geração de agenda **futura** ("começar amanhã"), não comparação de "hoje". Sem erro percebido.
- **`storage-cleanup` fora do `config.toml`** — não há `cron.schedule` que a chame no repo hoje; só vira problema se for agendada mandando `Bearer CRON_SECRET` em vez de JWT.
